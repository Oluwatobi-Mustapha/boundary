# --- Terraform core ---
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Terraform CLI local settings
.terraformrc
terraform.rc

# --- Plans ---
*.tfplan
*.tfplan.*

# --- Variables (often contain account IDs / secrets) ---
*.tfvars
*.tfvars.json
!*.tfvars.example
!*.auto.tfvars.example

# --- Overrides ---
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- Keep provider lockfile for reproducibility ---
# (Do NOT ignore this)
# .terraform.lock.hcl

# --- Credentials / env files (common in bootstraps) ---
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer

# AWS CLI / SDK creds sometimes copied locally (don’t commit)
.aws/
credentials
config

# --- Tool outputs / generated files ---
dist/
build/
out/
tmp/
*.zip

# --- Editors / OS ---
.DS_Store
.idea/
.vscode/
*.iml

# --- Logs ---
*.log
